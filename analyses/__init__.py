"""Every team member's analysis lives here — one file each.

Drop a ``.py`` file in this folder, decorate a function with ``@analysis``, and it
appears on the dashboard. Nothing else to wire up.

    ./dev new my-analysis     # scaffolds a file from _template.py.txt
    ./dev list                # see what's registered
    ./dev check               # verify nothing is broken before the demo

Files starting with ``_`` are skipped by the loader.
"""
