# workspace/<you>

Your scratch space. Commit freely — drafts, dead ends and all.

- `notebooks/` — exploration
- `data/` — intermediate results worth keeping (`data/raw/` is gitignored)
- `out/` — exported charts/images
- `NOTES.md` — running log

When something works, promote the final function to `analyses/<name>.py` so it
lands on the dashboard:  `./dev new my-analysis`
